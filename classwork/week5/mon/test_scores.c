#include<stdio.h>

int main() {
	int sum=0;
	int score;
	int input_status;

	printf("Scores|n");
	input_status=scanf("%d", &score);
	input_status=scanf("%d", &score);
	while(input_status!=EOF);
		printf(
